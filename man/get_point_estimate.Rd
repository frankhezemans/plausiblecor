% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_grid_stats.R
\name{get_point_estimate}
\alias{get_point_estimate}
\title{Compute Point Estimate from Discretised Density}
\usage{
get_point_estimate(
  val,
  dens,
  method = c("mean", "median", "quantile"),
  prob = NULL,
  dx = NULL
)
}
\arguments{
\item{val}{Numeric vector representing the grid of values over which the
density is defined.}

\item{dens}{Numeric vector of the same length as \code{val}, representing the
density at each grid point.}

\item{method}{Character string specifying which point estimate to compute.
One of \code{"mean"}, \code{"median"}, or \code{"quantile"}.}

\item{prob}{Numeric value between 0 and 1, only required when
\code{method = "quantile"}. Specifies the cumulative probability at which
to evaluate the quantile.}

\item{dx}{Grid spacing (step size). If \code{NULL} (default), it is estimated
from the unique values of \code{val} (grid points) using \code{\link[=diff]{diff()}}.}
}
\value{
A numeric value corresponding to the requested point estimate.
}
\description{
Internal helper function to compute a point estimate (mean,
median, or arbitrary quantile) from a numeric vector of values and a
corresponding density over a discretised grid.
}
\keyword{internal}
